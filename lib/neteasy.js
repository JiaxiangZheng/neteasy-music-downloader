+function () {
	exports.Neteasy = Neteasy;

	var router = {
	};

	function Neteasy(params) {
		this.init(params);
	}

	Neteasy.prototype.login = function (usrname, password) {

	};

	Neteasy.prototype.love = function () {
		if (!this.isLogin) {
			// 
		}
	};

	Neteasy.prototype.next = function () {

	};

	Neteasy.prototype.init = function (params) {
		// 设置默认参数
		params = params || ｛

		};

	};
}();
